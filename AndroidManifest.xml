<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DasteTaawun"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">

        <!-- ==================== SPLASH SCREEN ==================== -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DasteTaawun.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ==================== AUTHENTICATION ==================== -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- ==================== MAIN ACTIVITY ==================== -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:launchMode="singleTop" />

        <!-- ==================== CHARITY REQUESTS ==================== -->
        <activity
            android:name=".ApplyCharityActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".MainActivity" />

        <!-- ==================== VOLUNTEER ==================== -->
        <activity
            android:name=".VolunteerRegistrationActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".VolunteerListActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".MainActivity" />

        <!-- ==================== DONATIONS ==================== -->
        <activity
            android:name=".DonationActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".MaterialDonationActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".DonationActivity" />

        <activity
            android:name=".DonationHistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".ProfileActivity" />

        <!-- ==================== EVENTS ==================== -->
        <activity
            android:name=".EventsActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".EventDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".EventsActivity" />

        <!-- ==================== PROFILE ==================== -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".MainActivity" />

        <!-- ==================== ADMIN ==================== -->
        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.DasteTaawun.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".MainActivity" />

        <!-- ==================== FIREBASE SERVICES ==================== -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Firebase Metadata (Optional but recommended) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/primary_color" />

    </application>

</manifest>